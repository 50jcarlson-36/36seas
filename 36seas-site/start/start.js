const params=new URLSearchParams(location.search);const campaign=params.get('utm_campaign')||params.get('campaign')||'fb-organic';const source=params.get('utm_source')||'facebook';const interest=document.getElementById('interest');if(interest)interest.value=`FB author lead | ${source} | ${campaign}`.slice(0,120);function track(event,data={}){const payload={source,campaign,...data};if(typeof window.fbq==='function')window.fbq('trackCustom',event,payload);if(typeof window.gtag==='function')window.gtag('event',event,payload);window.dataLayer=window.dataLayer||[];window.dataLayer.push({event,...payload})}track('AuthorLandingView');document.querySelectorAll('[data-stage]').forEach(link=>link.addEventListener('click',()=>track('AuthorStageSelected',{stage:link.dataset.stage})));const manuscript=document.getElementById('manuscript-link');if(manuscript)manuscript.addEventListener('click',()=>track('ManuscriptIntent'));